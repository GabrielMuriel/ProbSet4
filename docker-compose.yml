services:
  frontend-service:
    build: ./frontend-node
    ports:
      - "8080:8080"
    networks:
      - enrollnet
  course-service:
    build: ./course-node
    ports:
      - "8082:8080"
    networks:
      - enrollnet
networks:
  enrollnet:
